apiVersion: v1
kind: Service
metadata:
  name: {{ include "axis-swgi-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "axis-swgi-api.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
